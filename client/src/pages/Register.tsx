import PageWrapper from '@components/PageWrapper';
import Stack from '@mui/material/Stack';

const Register = () => {
  return (
    <PageWrapper>
      <Stack spacing={2}>Register</Stack>
    </PageWrapper>
  );
};

export default Register;